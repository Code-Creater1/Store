# Store App

Basic Next.js e-commerce starter with:

- Next.js (App Router)
- MongoDB + Mongoose
- JWT auth
- Tailwind CSS

Local run:

1. Set `MONGODB_URI` and `JWT_SECRET` env vars (or use defaults).
2. Install deps: `npm install`
3. Run dev: `npm run dev`

APIs live under `/api/*`.
